#include <iostream>                         //30
#include <cmath>
using namespace std;
int main() {
	int x;
	cin >> x;
	cout << pow(x, 2) * pow(x, 2) << endl;
	cout << pow(x, 2) * pow(x, 2) * pow(x, 2) << endl;
	cout << pow(x, 3) * pow(x, 3) * pow(x, 3) * pow(x, 3) * pow(x, 3);
}